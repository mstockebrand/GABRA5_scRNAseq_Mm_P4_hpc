#  CLUSTERING
##  Clustering the cells
#### find neighbors
    Scn2a.P4 <- FindNeighbors(Scn2a.P4, reduction = "integrated.dr", dims = 1:last_pc_id)
#### find clusters
    Scn2a.P4 <- FindClusters(Scn2a.P4, resolution = 0.8) ## default resolution = 0.8
The default resolution set to 0.8 yielded surprisingly well clustering results (s. fig. below), so further resolution adjustments could be omitted.
##  UMAP non-linear dimensionality reduction
    Scn2a.P4 <- RunUMAP(Scn2a.P4, dims = 1:last_pc_id, reduction = "integrated.dr")
##  visualize clustering
    Idents(Scn2a.P4) <- "seurat_clusters"
    DimPlot(Scn2a.P4, reduction = "umap", label = T, label.size = 6, repel = T)

<p align="center">
<img src="https://github.com/mstockebrand/GABRA5_scRNAseq_Mm_P4_hpc/blob/main/Analysis Workflow/Figures/Clustering_Figs/Clustering_1_UMAP_clusters.png" width="800" height="627"/>
</p>

